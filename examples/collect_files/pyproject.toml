[project]
name = "collect-files"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyanura",
    "numpy",
]

[tool.uv.sources]
pyanura = { path = "../.." }

[dependency-groups]
dev = [
    "ty",
]
