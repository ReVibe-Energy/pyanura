[project]
name = "forwarder"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "pyanura",
    "paho-mqtt>=2.0.0",
]

[tool.uv.sources]
pyanura = { path = "../.." }

[dependency-groups]
dev = [
    "ty",
]
